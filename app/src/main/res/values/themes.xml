<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Setting" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="btnBg">

        <item name="android:onClick">onClick</item>
        <item name="android:layout_marginTop">1px</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="background">@drawable/drawable_selector</item>
<!--        <item name="drawableRight">@drawable/t1</item>-->
        <item name="android:drawableRight">@drawable/ic_baseline_arrow_forward_ios_24</item>

    </style>
</resources>